# DEVELOPMENT

# The container runs as a non-root user. Match these to your host account
# (`id -u` / `id -g`) so bind mounted files keep sane ownership.
LOCAL_USER_ID=1000
LOCAL_GROUP_ID=1000

# Baked into the development image's global git config
GIT_NAME=
GIT_EMAIL=
